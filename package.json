{
  "name": "rental-dapp",
  "version": "1.0.0",
  "type": "module",
  "description": "He thong quan ly thue nha bang smart contract",
  "scripts": {
    "build": "hardhat build",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat ignition deploy ignition/modules/RentalSystem.ts --network localhost --build-profile default && tsx scripts/sync-frontend-config.ts localhost",
    "deploy:sepolia": "hardhat ignition deploy ignition/modules/RentalSystem.ts --network sepolia --build-profile default && tsx scripts/sync-frontend-config.ts sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@types/mocha": "^10.0.0",
    "@types/node": "^22.0.0",
    "hardhat": "^3.11.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
